//A function takes an integer and a string
//Loop through the string
//keep a counter that checks the letters in the string until it finds a space
//Consider that a word
//add the word to a list of objects
//the object should have, as a key, the word and, as a value, the number of times it has appeared
//check all values, take the highest numbers based on the integer given.
function mostRecurringWords(string, int) {
  spaceIndex = [];
  for(var i = 0; i < string.length; i++) {
    if(string[i] == ' ') {
      spaceIndex.push(i);
      spaceIndex[0] - 0
    }
  }
  function addWordToObj() {
    if(spaceIndex.length == 1) {

    }
    spaceIndex[]
  }

}
